import React from 'react';
import { Button } from '../../components'
import { getColor } from '../../utils/colors'

export const Equips = () => {
  return (
    <div>
    <Button text="creitooo" color={getColor(1)} />
    </div>
  );
};
